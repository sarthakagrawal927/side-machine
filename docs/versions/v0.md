# v0.0.0 — specification baseline

The initial repository contains the implementation PRD and safety boundaries,
but no executable controller or worker helpers.

Its reference design is same-LAN native SSH, tmux, caffeinate, Git, Claude
Code, and Codex CLI. This tag exists so the implementation can always be
compared with the original requirements.
